# Apps

Application code lives here.

- `qr-menu/` - guest-facing QR menu React/Vite app served in production at `/mvp/`.

The landing is intentionally outside `apps/` in `landing/` because it has both source and production static build snapshots.

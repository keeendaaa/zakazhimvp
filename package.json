{
  "name": "zakazhi-production-backup",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:qr": "npm --prefix apps/qr-menu run build",
    "dev:qr": "npm --prefix apps/qr-menu run dev",
    "build:landing": "npm --prefix landing/source run build",
    "dev:landing": "npm --prefix landing/source run dev"
  }
}
